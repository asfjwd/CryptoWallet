<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CryptoWallet" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:textViewStyle">@style/NunitoTextViewStyle</item>
        <item name="android:buttonStyle">@style/NunitoButtonStyle</item>
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryVariant">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="NunitoTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/nunito</item>
    </style>

    <style name="NunitoButtonStyle" parent="android:Widget.Holo.Button">
        <item name="android:fontFamily">@font/nunito</item>
    </style>

    <style name="Theme.CryptoWallet.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.CryptoWallet.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.CryptoWallet.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>